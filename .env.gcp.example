# =============================================================================
# MWTeam - Configuration GCP
# =============================================================================
# Copiez ce fichier vers .env.local et remplissez les valeurs

# Base de données PostgreSQL (Cloud SQL ou URL externe)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# NextAuth configuration
NEXTAUTH_URL="https://votre-app.run.app"
NEXTAUTH_SECRET="générer-avec-openssl-rand-base64-32"

# =============================================================================
# Configuration LLM - GCP Vertex AI
# =============================================================================
# Provider: vertex (GCP) ou mistral (fallback)
LLM_PROVIDER="vertex"

# ID de votre projet GCP
GOOGLE_CLOUD_PROJECT="votre-projet-gcp"

# Région Vertex AI (europe-west1 recommandé pour la France)
VERTEX_AI_LOCATION="europe-west1"

# Modèle à utiliser (gemini-1.5-flash compatible crédits gratuits $300)
VERTEX_AI_MODEL="gemini-1.5-flash"

# =============================================================================
# Options de l'application
# =============================================================================
# Active le workflow multi-agents pour la génération de plannings
USE_AGENTIC_PLANNING="true"

# Environnement (development, production)
NODE_ENV="production"
